/ {
    aliases {
        led0 = &external_led;
    };

    leds {
        compatible = "gpio-leds";

        external_led: led_0 {
            gpios = <&gpio0 2 GPIO_ACTIVE_HIGH>;
            label = "External LED";
        };
    };
};